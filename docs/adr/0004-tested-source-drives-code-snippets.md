# 项目源码是正文代码片段的单一来源

凡是声称来自实践项目的代码，VitePress 都通过 `<<<` 和命名 region 从已测试的源文件导入，不在 Markdown 中复制第二份。这个约束要求项目代码为讲解保留清晰边界，却能让失效路径或 region 在站点构建时直接报错，并减少正文与可运行代码长期不一致的问题。

不属于实践项目的可执行短片段保存在独立 Dart 或 Flutter 文件中，并纳入静态分析或测试；伪代码明确标注。只有无法独立运行且语义直观的极短片段可以直接写在 Markdown 中。
