# 公开站点遵守可访问性基线且不追踪读者

VitePress 站点和 Flutter Web 独立预览以 WCAG 2.2 AA 作为设计与验收目标。站点必须支持键盘访问、可见焦点、语义化结构、足够对比度、文本缩放和窄屏阅读；实践项目还要把关键流程纳入键盘、语义与响应式检查。自动化工具只能覆盖其中一部分，发布前仍保留人工检查。

首版不加载访问分析、广告、评论、社交组件或第三方字体，不设置服务端账户。章节完成状态只保存在读者浏览器中；打开源码、项目预览或内容报错入口时，才由读者主动跳转到相应页面。以后若要加入遥测，必须先说明采集字段、用途、保留期限和退出方式，并重新评估本决定。
